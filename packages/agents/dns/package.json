{
  "name": "@fracta/agent-dns",
  "version": "0.1.0",
  "description": "Postura de e-mail/DNS (SPF, DKIM, DMARC) — anti-spoofing/phishing, determinístico",
  "license": "MIT",
  "author": "Anderson Gadelha",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fracta/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "*",
    "typescript": "*",
    "vitest": "*",
    "@types/node": "*"
  }
}
